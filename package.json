{
  "name": "html5-banner-generator",
  "version": "1.0.0",
  "description": "HTML5 banner project generator",
  "type": "module",
  "bin": {
    "banner-generator": "./bin/cli.js"
  },
  "scripts": {
    "gen": "banner-generator",
    "zip": "banner-generator --zip",
    "zip-only": "banner-generator --zip-only",
    "preview": "banner-generator --preview"
  },
  "dependencies": {
    "archiver": "^6.0.1",
    "fs-extra": "^11.2.0",
    "mustache": "^4.2.0"
  }
}
